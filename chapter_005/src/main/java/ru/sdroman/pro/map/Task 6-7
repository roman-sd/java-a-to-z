1.Рассказать про коллизии в Hashtable структурах и как они реализованы в Java

  - HashMap представляет из себя массив записей (Map.Entry<K,V>);
  - при добавлении новой записи <ключ, значение>, вычисляется хэшкод ключа;
  - по хэшкоду ключа находится номер элемента массива записей, в которой следует поместить новое значение;
  - если в этом элементе уже располагается какая-то запись, то существует 2 варианта: либо ключ совпадает,
   тогда следует заменить значение, любо совпал хэшкод (произошла коллизия), тогда элементом этого массива
   устанавливается новая запись, у которой есть поле, ссылающееся на старую запись.
   Таким образом, в следующий раз для просмотра записей в этом элементе необходимо будет пройти связанный
   список записей <ключ, значение>.


2. Как вычисляются hash функция.

  - Берется число, отличное от нуля, к примеру 17. Для удобства.
  Присваиваем переменной result это начальное значение.
  - Для каждого поля вычисляется hashCode с.
  - Скомбинировать result и полученный hashCode с:
  result = result * q + c, где q = 37 нечетное и простое(например).
  - Вернуть result.